CREATE TABLE weeks (
    [index]        INTEGER PRIMARY KEY AUTOINCREMENT
                           NOT NULL,
    date           DATE,
    sumTotal       TEXT,
    countOfViewers TEXT,
    link           TEXT
);
